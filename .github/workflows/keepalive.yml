name: keepalive-supabase
on:
  schedule:
    - cron: '17 6 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase para evitar pausa
        run: |
          curl -sS -o /dev/null -w "%{http_code}\n" "https://nopfmljryhhjispoukbc.supabase.co/rest/v1/registro_pagos?select=id&limit=1" -H "apikey: sb_publishable_hD7nOJwuAuceBX3A2mu4kA_SljIMd7V"
